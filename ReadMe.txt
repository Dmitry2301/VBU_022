TODO:
В проект NullTerminatedLines добавить функции:
void UpperCase(char str[]);	//Переводит строку в верхний регистр - все буквы в строке делает большими;
void LowerCase(char str[]);	//Переводит строку в нижний регистр - все буквы в строке делает маленькими;
void Capitalize(char str[]);	//Каждое слово в предложении делает с большой буквы;
void Shrink(char str[]);	//Удаляет        лишние    пробелы        между  словами в   предложении;
------------------------------------------------------------------------------------------------------------
bool isPalindrome(char str[]);	//Определят, является ли строка палинромом (12321, или, ого, Аргентина манит негра);

bool isNumber(char str[]);	//Определяет, является ли, чтрока числом;
int  toNumber(char str[]);	//Преобразует строку в число;
-------------------------------------------------------------------------------------------------------------
??? Dec2Bin(int decimal);	//Десятичное число преобразует в двоичную систему счисления;
??? Bin2Dec(char binary[]);	//Двоичное число преобразует в десятичную систему счисления;
??? Dec2Hex(int decimal);	//Десятичное число преобразует в Шестнадцатеричную систему счисления;
??? Hex2Dec(char hex[]);	//Шестнвдцатеричное число преобразует в десятичную СС;
